/* function ColorText({ color }) {
  return (
    <h2 style={{ color: color }}>
      This text is {color}
    </h2>
  );
}

export default ColorText; */

function ColorText({ color }) {
  return (
    <h2 style={{ color: color }}>
      This text color is {color}
    </h2>
  );
}

export default ColorText;